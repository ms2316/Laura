function [f] = pow_memo(a, b)
    if (b == 0)
        f = 1;
    else
        if isnan( saved(b) )
        if (mod(b,2) == 1)
            f = a*pow_memo(a, b-1);
        else
            temp = pow_memo(a, b / 2);
            f = temp * temp;
        end
    end